{{yield}}
{{#if saving}}
  <button class="btn btn-primary btn-disabled"><i class="icon icon-spinner icon-spin"></i> {{t savingLabel}}</button>
{{else}}
  <button {{action "save"}} disabled={{saveDisabled}} class="btn btn-primary {{if saved savedColor saveColor}}">
    {{t btnLabel}}
  </button>
{{/if}}
